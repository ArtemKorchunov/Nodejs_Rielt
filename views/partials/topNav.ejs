<nav class="navbar navbar-toggleable-md navbar-inverse fixed-top bg-inverse">
    <button class="navbar-toggler navbar-toggler-right hidden-lg-up" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand" href="#">Rielt</a>

    <div class="collapse navbar-collapse" id="navbarsExampleDefault">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link top-home" href="/">Home<span class="sr-only">(current)</span></a>
            </li>
            <% if (user && user.right_level < 5) {%>
            <li class="nav-item">
                <a class="nav-link top-settings" href="/user/settings/change-profile">Settings</a>
            </li>
            <% }%>
            <li class="nav-item">
                <% if (user && user.right_level >= 5) {%>
                <a class="nav-link top-control-panel" href="/admin/control-panel/add-user">Control Panel</a>
                <% } else if (user && user.right_level < 5) { %>
                <a class="nav-link top-control-panel" href="/user/control-panel/add-flat">Control Panel</a>
                <% } %>
            </li>
            <li class="nav-item">
                <% if (!user) {%>
                <a class="nav-link" href="/login">Log in</a>
                <% } else {%>
                <a class="nav-link" href="/logout">Log out</a>
                <% } %>
            </li>
        </ul>
    </div>
</nav>
<script>
(function () {
    //$("li[class='nav-item active']").removeClass('active');
    var e = document.location.pathname.split('/')[2];
    e = e ? e : 'home'
    var d = $("a.top-" + e);
    d.parent().addClass('active');
})()
</script>